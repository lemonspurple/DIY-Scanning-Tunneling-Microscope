name: Build STM_GUI

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Python Setup
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: patchelf installieren (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: sudo apt-get install -y patchelf

    - name: Dependencies installieren
      run: pip install nuitka -r Software/tkinter-usb-app/requirements.txt

    - name: Binary bauen (Linux/Mac)
      if: matrix.os != 'windows-latest'
      run: |
        cd Software/tkinter-usb-app/src
        nuitka --onefile \
          --include-package=gui \
          --include-data-dir=assets=assets \
          --include-data-files=config.ini=config.ini \
          --output-filename=STM_GUI \
          main.py

    - name: Binary bauen (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        cd Software/tkinter-usb-app/src
        nuitka --onefile --include-package=gui --include-data-dir=assets=assets --include-data-files=config.ini=config.ini --output-filename=STM_GUI main.py

    - name: Binary hochladen
      uses: actions/upload-artifact@v4
      with:
        name: "STM_GUI-${{ matrix.os }}"
        path: Software/tkinter-usb-app/src/STM_GUI*
